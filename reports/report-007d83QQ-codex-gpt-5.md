## [2026-08-19 16:29] Task Record

### Task Description
- Rebuild the first phase of the experiments API after an accidental hard reset.
- Implement only experiment CRUD/list/status behavior; do not implement participant or course APIs.
- Use migration 14 for the experiment schema and create the participant/course join tables for future work.

### Actions Taken
- Added `internal/database/migrations/14_experiments.up.sql` and `internal/database/migrations/14_experiments.down.sql` for the experiment status enum, experiments table, and both join tables.
- Added the `internal/experiment` package with sqlc schema/queries, repository store, service, HTTP handler, validation, errors, and table-driven handler/service tests.
- Registered the experiment handler in `cmd/backend/main.go`.
- Regenerated sqlc files with `make gen`; because the project generates every package from `internal/database/full_schema.sql`, this also updated `models.go` in auth, chat, content, question, and user.
- Reviewed `../sciedu-api/service/experiments.tsp` and implemented only list, create, detail, update, and update-status routes.
- Executed `make gen`, `gofmt`, focused and related Go tests, race tests, `go vet`, `golangci-lint`, and `git diff --check`.

### Attempted Methods
- Kept the experiment domain in the required Handler -> Service -> Repository dependency direction.
- Used pointer request fields so required JSON fields, including boolean and zero-valued fields, can be distinguished from omitted fields.
- Used the existing authorization middleware with `EXPERIMENTER` and `ADMIN` roles.
- Queried the join tables only for `participantCount` and `courseCount`; no participant/course mutation endpoints were added.
- Tried to connect to the local Docker daemon to run a PostgreSQL migration round-trip. The sandbox initially denied socket access; after approval, Docker reported that the daemon was not running.
- Ran `go test ./...`; experiment and relevant packages passed, while unrelated existing chat/auth tests could not complete because `internal/chat/mocks` lacks the `github.com/stretchr/objx` go.sum entry and sandboxed tests cannot bind `httptest` listeners.

### Issues & Blockers
- A live PostgreSQL migration test was not possible because the local Docker daemon was unavailable.
- The repository currently has no `courses` table migration. Therefore `experiment_courses.course_id` is present with the composite primary key and experiment cascade FK, but intentionally has no course FK yet. Adding that FK before the courses table exists would make migration 14 fail.
- `internal/experiment/queries.sql.go` is generated by sqlc and ignored by the repository's existing `.gitignore`; a clean checkout must run `make gen`, consistent with the existing project workflow.

### Next Steps
- When the course-domain migration is added, add an `experiment_courses.course_id -> courses.id ON DELETE CASCADE` foreign key in that migration.
- Start Docker/PostgreSQL and run the full migration chain to verify migration 14 against a real database.
- Implement current-experiment, participant assignment, and course assignment APIs only when those later phases are requested.
